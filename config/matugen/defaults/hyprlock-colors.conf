# Default palette, copied into place before Matugen generates the first theme.
# The writable runtime copy is then owned by Matugen; see ../README.md.
$accent  = rgb(adc6ff)
$surface = rgb(111318)
$text    = rgb(e2e2e9)
$red     = rgb(ffb4ab)
$yellow  = rgb(bfc6dc)
